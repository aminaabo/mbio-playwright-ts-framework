{
  "name": "mbio-web-ui-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright TypeScript automation framework for mb.io public web UI scenarios.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^26.1.0",
    "typescript": "^5.5.0"
  }
}
